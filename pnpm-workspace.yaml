allowBuilds:
  esbuild: true

# Keep dependency installs from accepting packages immediately after publish.
# Renovate waits longer than this before opening PRs, so dependency PRs should
# not fail CI only because a package is still inside the release-age window.
minimumReleaseAge: 1440
